/* Programme de test de ls.c */


#include <stdio.h>
#include <stdlib.h>

void ls_function(int argc, char *argv[]);

int main () {
    printf("Test");
/*     int argc=5;
    char* commande;
    commande = malloc(sizeof(char) * 20);
    commande = "ls";


    ls_function(1, &commande); */

    return 0;
}