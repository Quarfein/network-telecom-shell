#include <stdio.h>
#include <string.h>
void deconca( char* Dc[]) 
{
  int i,j,k,q;
  //extraire la commande
  
  for (i=0;i<strln(Dc);i++)
  {
    if DC[i]==' '
    {
      commandde=(char*)malloc(size of(char)*i); // la chaine pour la commande
      int j
      for(j=0;j<i;j++;) 
      {
        Dc[j]=commande[j] //stocker la commande
        Dc=Dc.replace(Dc[j], string.empty); //suprimer la commande de la chaine
      }
      Dc=Dc.replace(Dc[i], string.empty); //supprimer l'espace
    }
    break; 
   }
    k=0;
    
    // extraire les options
    
   for (i=0;i<strln(Dc);i++)
   {
    // determiner le nombre des options
     if (Dc[i]=' ')
     {
       k++
     } 
     q=0
     for (i=0;i<strln(Dc);i++)
     {
       options=(char*)malloc(size of(char)*k); // tableau avec le nombre des options
       if DC[i]==' '
       {
         option=(char*)malloc(size of(char)*i+1); // la chaine pour l'option
         for(j=0;j<i;j++;)
         {
           Dc[j]=option[j] // extraire l'option
           Dc=Dc.replace(Dc[j], string.empty); //supprimer l'option de la chaine
         }
         q=q+1
         Dc=Dc.replace(Dc[i], string.empty); //supprimer l'espace
         options[k]=option  //ranger l'option dans le tableau
       } 
     }
}
int main()
{
  deconca(Dc[])
  return(0);
}
