#include <stdio.h>
#include <string.h>
void deconca( char* Dc[]){
  int i,j,k;
  char d[]=" ";
  int len = strlen(Dc);
  k=0;
  for (i=0;i<strln(Dc);i++) // le nombre des parametres
  {
    if (Dc[i]=' ')
      {
        k++
      } 
   }
   i=0
   while(Dc[i]<>' ')
   {
     i++
   }
   commande=(char*)malloc(size of(char)*i);
   char *p = strtok(Dc, d); //extraire la commande
   strcpy(p,commande);   // ranger la variable
   for(j=0;j<=i;j++;) 
     {
       Dc=Dc.replace(Dc[j], string.empty); //suprimer la commande de la chaine
      }
    }
   
  str=(char*)malloc(size of(char)*k); //tableau des options
  for (i=0;i<len);i++) // extraire les options
  {
    char *p = strtok(Dc, d);
    while(p != NULL)
    {
     str[i]=p;
     p = strtok(NULL, d);
    }
  }
int main(){
  deconca(Dc[])
  return(0);
}
